<!DOCTYPE html>
<html>
<head>
    <title>Web记事本</title>
    <!-- 引入 Bootstrap 样式文件 -->
    <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.0.2/css/bootstrap.min.css"
          integrity="sha384-MXeQqQ2+bsnWtLh2CKXXC6t+5txn/GmyrBpJnbzr+9t+k5f5/Ua0Jt+ZszwHi1h2" crossorigin="anonymous">
    <style>
        body {
            background-color: #f2f2f2;
        }

        .container {
            margin-top: 50px;
        }

        .list-group-item {
            background-color: #ffffff;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 10px;
            margin-bottom: 10px;
        }

        .list-group-item:hover {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .form-control {
            background-color: #ffffff;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 10px;
            margin-bottom: 10px;
            color: #333333;
        }

        .form-control:focus {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .btn-primary {
            background-color: #0d6efd;
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            margin-top: 10px;
        }

        .btn-primary:hover {
            background-color: #0c63e4;
        }

        .btn-danger {
            background-color: #dc3545;
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            margin-top: 10px;
        }

        .btn-danger:hover {
            background-color: #c82333;
        }

        .btn-group {
            display: flex;
            justify-content: center;
        }

        .btn-group > button {
            margin-right: 10px;
        }

        .btn-group > button:last-child {
            margin-right: 0;
        }

        .text-center {
            text-align: center;
        }

        .text-right {
            text-align: right;
        }

        .text-left {
            text-align: left;
        }

        .text-bold {
            font-weight: bold;
        }

        .text-italic {
            font-style: italic;
        }

        .text-underline {
            text-decoration-line: underline;
        }

        .text-strikeout {
            text-decoration-line: line-through;
        }

        .text-overline {
            text-decoration-line: overline;
        }

        .text-red {
            color: red;
        }

        .text-green {
            color: green;
        }

        .text-blue {
            color: #0000FF;
        }

        .text-yellow {
            color: #FFFF00;
        }

    </style>
</head>
<body>
<div class="container">
    <h1 class="my-5 text-center fw-bold">Web记事本</h1>
    {% if notes %}
        <ul class="list-group">
            {% for note in notes %}
                <li class="list-group-item">
                    <div class="row">
                        <div class="col-10">
                            {{ note }}
                        </div>
                        <div class="col-2 text-right">
                            <form method="post" action="{{ url_for('delete') }}">
                                <input type="hidden" name="id" value="{{ note }}">
                                <button type="submit" class="btn btn-danger">删除</button>
                            </form>
                        </div>
                    </div>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p class="my-5 text-center">暂无内容</p>
    {% endif %}
    <form method="post" action="{{ url_for('add') }}" class="mt-5">
        <div class="mb-3">
            <input type="text" name="note" id="note" class="form-control text-center" placeholder="添加新笔记...">
        </div>
        <button type="submit" class="btn btn-primary">添加笔记</button>
    </form>
</div>
<!-- 引入 Bootstrap JavaScript 文件 -->
<script src="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.0.2/js/bootstrap.bundle.min.js"
        integrity="sha384-Cz6gu72l6r50w44rW/jMfTnjnAZzWeH3TeyNI8W2JgKXYDyLs6LVe/G/KTEWdLy"
        crossorigin="anonymous"></script>
</body>
</html>
